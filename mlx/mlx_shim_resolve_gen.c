//go:build darwin && arm64 && cgo

// GENERATED by scripts/gen_mlxc_shim.py — do not edit.
#include <dlfcn.h>

#include "mlx_shim_dispatch_gen.h"

int mlx_shim_resolve_dispatch(void* handle, mlx_shim_dispatch_t* d) {
  void* p;
  p = dlsym(handle, "mlx_abs");
  if (!p) return 0; d->mlx_abs = (int (*)(mlx_array* res, const mlx_array a, const mlx_stream s))p;
  p = dlsym(handle, "mlx_add");
  if (!p) return 0; d->mlx_add = (int (*)(mlx_array* res, const mlx_array a, const mlx_array b, const mlx_stream s))p;
  p = dlsym(handle, "mlx_arange");
  if (!p) return 0; d->mlx_arange = (int (*)(mlx_array* res, double start, double stop, double step, mlx_dtype dtype, const mlx_stream s))p;
  p = dlsym(handle, "mlx_argmax");
  if (!p) return 0; d->mlx_argmax = (int (*)(mlx_array* res, const mlx_array a, bool keepdims, const mlx_stream s))p;
  p = dlsym(handle, "mlx_argmax_axis");
  if (!p) return 0; d->mlx_argmax_axis = (int (*)(mlx_array* res, const mlx_array a, int axis, bool keepdims, const mlx_stream s))p;
  p = dlsym(handle, "mlx_argpartition_axis");
  if (!p) return 0; d->mlx_argpartition_axis = (int (*)(mlx_array* res, const mlx_array a, int kth, int axis, const mlx_stream s))p;
  p = dlsym(handle, "mlx_array_data_float32");
  if (!p) return 0; d->mlx_array_data_float32 = (const float* (*)(const mlx_array arr))p;
  p = dlsym(handle, "mlx_array_data_int64");
  if (!p) return 0; d->mlx_array_data_int64 = (const int64_t* (*)(const mlx_array arr))p;
  p = dlsym(handle, "mlx_array_data_uint32");
  if (!p) return 0; d->mlx_array_data_uint32 = (const uint32_t* (*)(const mlx_array arr))p;
  p = dlsym(handle, "mlx_array_data_uint8");
  if (!p) return 0; d->mlx_array_data_uint8 = (const uint8_t* (*)(const mlx_array arr))p;
  p = dlsym(handle, "mlx_array_dtype");
  if (!p) return 0; d->mlx_array_dtype = (mlx_dtype (*)(const mlx_array arr))p;
  p = dlsym(handle, "mlx_array_eval");
  if (!p) return 0; d->mlx_array_eval = (int (*)(mlx_array arr))p;
  p = dlsym(handle, "mlx_array_free");
  if (!p) return 0; d->mlx_array_free = (int (*)(mlx_array arr))p;
  p = dlsym(handle, "mlx_array_ndim");
  if (!p) return 0; d->mlx_array_ndim = (size_t (*)(const mlx_array arr))p;
  p = dlsym(handle, "mlx_array_new");
  if (!p) return 0; d->mlx_array_new = (mlx_array (*)(void))p;
  p = dlsym(handle, "mlx_array_new_data");
  if (!p) return 0; d->mlx_array_new_data = (mlx_array (*)(const void* data, const int* shape, int dim, mlx_dtype dtype))p;
  p = dlsym(handle, "mlx_array_new_int");
  if (!p) return 0; d->mlx_array_new_int = (mlx_array (*)(int val))p;
  p = dlsym(handle, "mlx_array_set");
  if (!p) return 0; d->mlx_array_set = (int (*)(mlx_array* arr, const mlx_array src))p;
  p = dlsym(handle, "mlx_array_shape");
  if (!p) return 0; d->mlx_array_shape = (const int* (*)(const mlx_array arr))p;
  p = dlsym(handle, "mlx_array_size");
  if (!p) return 0; d->mlx_array_size = (size_t (*)(const mlx_array arr))p;
  p = dlsym(handle, "mlx_astype");
  if (!p) return 0; d->mlx_astype = (int (*)(mlx_array* res, const mlx_array a, mlx_dtype dtype, const mlx_stream s))p;
  p = dlsym(handle, "mlx_async_eval");
  if (!p) return 0; d->mlx_async_eval = (int (*)(const mlx_vector_array outputs))p;
  p = dlsym(handle, "mlx_clear_cache");
  if (!p) return 0; d->mlx_clear_cache = (int (*)(void))p;
  p = dlsym(handle, "mlx_closure_apply");
  if (!p) return 0; d->mlx_closure_apply = (int (*)(mlx_vector_array* res, mlx_closure cls, const mlx_vector_array input))p;
  p = dlsym(handle, "mlx_closure_free");
  if (!p) return 0; d->mlx_closure_free = (int (*)(mlx_closure cls))p;
  p = dlsym(handle, "mlx_closure_new_func_payload");
  if (!p) return 0; d->mlx_closure_new_func_payload = (mlx_closure (*)(int (*fun)(mlx_vector_array*, const mlx_vector_array, void*), void* payload, void (*dtor)(void*)))p;
  p = dlsym(handle, "mlx_compile");
  if (!p) return 0; d->mlx_compile = (int (*)(mlx_closure* res, const mlx_closure fun, bool shapeless))p;
  p = dlsym(handle, "mlx_concatenate_axis");
  if (!p) return 0; d->mlx_concatenate_axis = (int (*)(mlx_array* res, const mlx_vector_array arrays, int axis, const mlx_stream s))p;
  p = dlsym(handle, "mlx_conv1d");
  if (!p) return 0; d->mlx_conv1d = (int (*)(mlx_array* res, const mlx_array input, const mlx_array weight, int stride, int padding, int dilation, int groups, const mlx_stream s))p;
  p = dlsym(handle, "mlx_cos");
  if (!p) return 0; d->mlx_cos = (int (*)(mlx_array* res, const mlx_array a, const mlx_stream s))p;
  p = dlsym(handle, "mlx_default_gpu_stream_new");
  if (!p) return 0; d->mlx_default_gpu_stream_new = (mlx_stream (*)(void))p;
  p = dlsym(handle, "mlx_dequantize");
  if (!p) return 0; d->mlx_dequantize = (int (*)(mlx_array* res, const mlx_array w, const mlx_array scales, const mlx_array biases , mlx_optional_int group_size, mlx_optional_int bits, const char* mode, const mlx_array global_scale , mlx_optional_dtype dtype, const mlx_stream s))p;
  p = dlsym(handle, "mlx_device_free");
  if (!p) return 0; d->mlx_device_free = (int (*)(mlx_device dev))p;
  p = dlsym(handle, "mlx_device_is_available");
  if (!p) return 0; d->mlx_device_is_available = (int (*)(bool* avail, mlx_device dev))p;
  p = dlsym(handle, "mlx_device_new_type");
  if (!p) return 0; d->mlx_device_new_type = (mlx_device (*)(mlx_device_type type, int index))p;
  p = dlsym(handle, "mlx_divide");
  if (!p) return 0; d->mlx_divide = (int (*)(mlx_array* res, const mlx_array a, const mlx_array b, const mlx_stream s))p;
  p = dlsym(handle, "mlx_enable_compile");
  if (!p) return 0; d->mlx_enable_compile = (int (*)(void))p;
  p = dlsym(handle, "mlx_equal");
  if (!p) return 0; d->mlx_equal = (int (*)(mlx_array* res, const mlx_array a, const mlx_array b, const mlx_stream s))p;
  p = dlsym(handle, "mlx_exp");
  if (!p) return 0; d->mlx_exp = (int (*)(mlx_array* res, const mlx_array a, const mlx_stream s))p;
  p = dlsym(handle, "mlx_fast_metal_kernel_apply");
  if (!p) return 0; d->mlx_fast_metal_kernel_apply = (int (*)(mlx_vector_array* outputs, mlx_fast_metal_kernel cls, const mlx_vector_array inputs, const mlx_fast_metal_kernel_config config, const mlx_stream stream))p;
  p = dlsym(handle, "mlx_fast_metal_kernel_config_add_output_arg");
  if (!p) return 0; d->mlx_fast_metal_kernel_config_add_output_arg = (int (*)(mlx_fast_metal_kernel_config cls, const int* shape, size_t size, mlx_dtype dtype))p;
  p = dlsym(handle, "mlx_fast_metal_kernel_config_add_template_arg_bool");
  if (!p) return 0; d->mlx_fast_metal_kernel_config_add_template_arg_bool = (int (*)(mlx_fast_metal_kernel_config cls, const char* name, bool value))p;
  p = dlsym(handle, "mlx_fast_metal_kernel_config_add_template_arg_dtype");
  if (!p) return 0; d->mlx_fast_metal_kernel_config_add_template_arg_dtype = (int (*)(mlx_fast_metal_kernel_config cls, const char* name, mlx_dtype dtype))p;
  p = dlsym(handle, "mlx_fast_metal_kernel_config_add_template_arg_int");
  if (!p) return 0; d->mlx_fast_metal_kernel_config_add_template_arg_int = (int (*)(mlx_fast_metal_kernel_config cls, const char* name, int value))p;
  p = dlsym(handle, "mlx_fast_metal_kernel_config_free");
  if (!p) return 0; d->mlx_fast_metal_kernel_config_free = (void (*)(mlx_fast_metal_kernel_config cls))p;
  p = dlsym(handle, "mlx_fast_metal_kernel_config_new");
  if (!p) return 0; d->mlx_fast_metal_kernel_config_new = (mlx_fast_metal_kernel_config (*)(void))p;
  p = dlsym(handle, "mlx_fast_metal_kernel_config_set_grid");
  if (!p) return 0; d->mlx_fast_metal_kernel_config_set_grid = (int (*)(mlx_fast_metal_kernel_config cls, int grid1, int grid2, int grid3))p;
  p = dlsym(handle, "mlx_fast_metal_kernel_config_set_init_value");
  if (!p) return 0; d->mlx_fast_metal_kernel_config_set_init_value = (int (*)(mlx_fast_metal_kernel_config cls, float value))p;
  p = dlsym(handle, "mlx_fast_metal_kernel_config_set_thread_group");
  if (!p) return 0; d->mlx_fast_metal_kernel_config_set_thread_group = (int (*)(mlx_fast_metal_kernel_config cls, int thread1, int thread2, int thread3))p;
  p = dlsym(handle, "mlx_fast_metal_kernel_free");
  if (!p) return 0; d->mlx_fast_metal_kernel_free = (void (*)(mlx_fast_metal_kernel cls))p;
  p = dlsym(handle, "mlx_fast_metal_kernel_new");
  if (!p) return 0; d->mlx_fast_metal_kernel_new = (mlx_fast_metal_kernel (*)(const char* name, const mlx_vector_string input_names, const mlx_vector_string output_names, const char* source, const char* header, bool ensure_row_contiguous, bool atomic_outputs))p;
  p = dlsym(handle, "mlx_fast_rms_norm");
  if (!p) return 0; d->mlx_fast_rms_norm = (int (*)(mlx_array* res, const mlx_array x, const mlx_array weight , float eps, const mlx_stream s))p;
  p = dlsym(handle, "mlx_fast_rope");
  if (!p) return 0; d->mlx_fast_rope = (int (*)(mlx_array* res, const mlx_array x, int dims, bool traditional, mlx_optional_float base, float scale, int offset, const mlx_array freqs , const mlx_stream s))p;
  p = dlsym(handle, "mlx_fast_rope_dynamic");
  if (!p) return 0; d->mlx_fast_rope_dynamic = (int (*)(mlx_array* res, const mlx_array x, int dims, bool traditional, mlx_optional_float base, float scale, const mlx_array offset, const mlx_array freqs , const mlx_stream s))p;
  p = dlsym(handle, "mlx_fast_scaled_dot_product_attention");
  if (!p) return 0; d->mlx_fast_scaled_dot_product_attention = (int (*)(mlx_array* res, const mlx_array queries, const mlx_array keys, const mlx_array values, float scale, const char* mask_mode, const mlx_array mask_arr , const mlx_array sinks , const mlx_stream s))p;
  p = dlsym(handle, "mlx_gather");
  if (!p) return 0; d->mlx_gather = (int (*)(mlx_array* res, const mlx_array a, const mlx_vector_array indices, const int* axes, size_t axes_num, const int* slice_sizes, size_t slice_sizes_num, const mlx_stream s))p;
  p = dlsym(handle, "mlx_gather_qmm");
  if (!p) return 0; d->mlx_gather_qmm = (int (*)(mlx_array* res, const mlx_array x, const mlx_array w, const mlx_array scales, const mlx_array biases , const mlx_array lhs_indices , const mlx_array rhs_indices , bool transpose, mlx_optional_int group_size, mlx_optional_int bits, const char* mode, bool sorted_indices, const mlx_stream s))p;
  p = dlsym(handle, "mlx_gather_single");
  if (!p) return 0; d->mlx_gather_single = (int (*)(mlx_array* res, const mlx_array a, const mlx_array indices, int axis, const int* slice_sizes, size_t slice_sizes_num, const mlx_stream s))p;
  p = dlsym(handle, "mlx_get_active_memory");
  if (!p) return 0; d->mlx_get_active_memory = (int (*)(size_t* res))p;
  p = dlsym(handle, "mlx_get_cache_memory");
  if (!p) return 0; d->mlx_get_cache_memory = (int (*)(size_t* res))p;
  p = dlsym(handle, "mlx_get_default_device");
  if (!p) return 0; d->mlx_get_default_device = (int (*)(mlx_device* dev))p;
  p = dlsym(handle, "mlx_get_default_stream");
  if (!p) return 0; d->mlx_get_default_stream = (int (*)(mlx_stream* stream, mlx_device dev))p;
  p = dlsym(handle, "mlx_get_memory_limit");
  if (!p) return 0; d->mlx_get_memory_limit = (int (*)(size_t* res))p;
  p = dlsym(handle, "mlx_get_peak_memory");
  if (!p) return 0; d->mlx_get_peak_memory = (int (*)(size_t* res))p;
  p = dlsym(handle, "mlx_less_equal");
  if (!p) return 0; d->mlx_less_equal = (int (*)(mlx_array* res, const mlx_array a, const mlx_array b, const mlx_stream s))p;
  p = dlsym(handle, "mlx_log");
  if (!p) return 0; d->mlx_log = (int (*)(mlx_array* res, const mlx_array a, const mlx_stream s))p;
  p = dlsym(handle, "mlx_log1p");
  if (!p) return 0; d->mlx_log1p = (int (*)(mlx_array* res, const mlx_array a, const mlx_stream s))p;
  p = dlsym(handle, "mlx_matmul");
  if (!p) return 0; d->mlx_matmul = (int (*)(mlx_array* res, const mlx_array a, const mlx_array b, const mlx_stream s))p;
  p = dlsym(handle, "mlx_max_axes");
  if (!p) return 0; d->mlx_max_axes = (int (*)(mlx_array* res, const mlx_array a, const int* axes, size_t axes_num, bool keepdims, const mlx_stream s))p;
  p = dlsym(handle, "mlx_maximum");
  if (!p) return 0; d->mlx_maximum = (int (*)(mlx_array* res, const mlx_array a, const mlx_array b, const mlx_stream s))p;
  p = dlsym(handle, "mlx_mean_axes");
  if (!p) return 0; d->mlx_mean_axes = (int (*)(mlx_array* res, const mlx_array a, const int* axes, size_t axes_num, bool keepdims, const mlx_stream s))p;
  p = dlsym(handle, "mlx_metal_start_capture");
  if (!p) return 0; d->mlx_metal_start_capture = (int (*)(const char* path))p;
  p = dlsym(handle, "mlx_metal_stop_capture");
  if (!p) return 0; d->mlx_metal_stop_capture = (int (*)(void))p;
  p = dlsym(handle, "mlx_multiply");
  if (!p) return 0; d->mlx_multiply = (int (*)(mlx_array* res, const mlx_array a, const mlx_array b, const mlx_stream s))p;
  p = dlsym(handle, "mlx_negative");
  if (!p) return 0; d->mlx_negative = (int (*)(mlx_array* res, const mlx_array a, const mlx_stream s))p;
  p = dlsym(handle, "mlx_pad");
  if (!p) return 0; d->mlx_pad = (int (*)(mlx_array* res, const mlx_array a, const int* axes, size_t axes_num, const int* low_pad_size, size_t low_pad_size_num, const int* high_pad_size, size_t high_pad_size_num, const mlx_array pad_value, const char* mode, const mlx_stream s))p;
  p = dlsym(handle, "mlx_power");
  if (!p) return 0; d->mlx_power = (int (*)(mlx_array* res, const mlx_array a, const mlx_array b, const mlx_stream s))p;
  p = dlsym(handle, "mlx_quantize");
  if (!p) return 0; d->mlx_quantize = (int (*)(mlx_vector_array* res, const mlx_array w, mlx_optional_int group_size, mlx_optional_int bits, const char* mode, const mlx_array global_scale , const mlx_stream s))p;
  p = dlsym(handle, "mlx_quantized_matmul");
  if (!p) return 0; d->mlx_quantized_matmul = (int (*)(mlx_array* res, const mlx_array x, const mlx_array w, const mlx_array scales, const mlx_array biases , bool transpose, mlx_optional_int group_size, mlx_optional_int bits, const char* mode, const mlx_stream s))p;
  p = dlsym(handle, "mlx_repeat_axis");
  if (!p) return 0; d->mlx_repeat_axis = (int (*)(mlx_array* res, const mlx_array arr, int repeats, int axis, const mlx_stream s))p;
  p = dlsym(handle, "mlx_reset_peak_memory");
  if (!p) return 0; d->mlx_reset_peak_memory = (int (*)(void))p;
  p = dlsym(handle, "mlx_reshape");
  if (!p) return 0; d->mlx_reshape = (int (*)(mlx_array* res, const mlx_array a, const int* shape, size_t shape_num, const mlx_stream s))p;
  p = dlsym(handle, "mlx_rsqrt");
  if (!p) return 0; d->mlx_rsqrt = (int (*)(mlx_array* res, const mlx_array a, const mlx_stream s))p;
  p = dlsym(handle, "mlx_set_cache_limit");
  if (!p) return 0; d->mlx_set_cache_limit = (int (*)(size_t* res, size_t limit))p;
  p = dlsym(handle, "mlx_set_compile_mode");
  if (!p) return 0; d->mlx_set_compile_mode = (int (*)(mlx_compile_mode mode))p;
  p = dlsym(handle, "mlx_set_error_handler");
  if (!p) return 0; d->mlx_set_error_handler = (void (*)(mlx_error_handler_func handler, void* data, void (*dtor)(void*)))p;
  p = dlsym(handle, "mlx_set_memory_limit");
  if (!p) return 0; d->mlx_set_memory_limit = (int (*)(size_t* res, size_t limit))p;
  p = dlsym(handle, "mlx_sigmoid");
  if (!p) return 0; d->mlx_sigmoid = (int (*)(mlx_array* res, const mlx_array a, const mlx_stream s))p;
  p = dlsym(handle, "mlx_sin");
  if (!p) return 0; d->mlx_sin = (int (*)(mlx_array* res, const mlx_array a, const mlx_stream s))p;
  p = dlsym(handle, "mlx_slice");
  if (!p) return 0; d->mlx_slice = (int (*)(mlx_array* res, const mlx_array a, const int* start, size_t start_num, const int* stop, size_t stop_num, const int* strides, size_t strides_num, const mlx_stream s))p;
  p = dlsym(handle, "mlx_slice_update");
  if (!p) return 0; d->mlx_slice_update = (int (*)(mlx_array* res, const mlx_array src, const mlx_array update, const int* start, size_t start_num, const int* stop, size_t stop_num, const int* strides, size_t strides_num, const mlx_stream s))p;
  p = dlsym(handle, "mlx_softmax");
  if (!p) return 0; d->mlx_softmax = (int (*)(mlx_array* res, const mlx_array a, bool precise, const mlx_stream s))p;
  p = dlsym(handle, "mlx_softmax_axis");
  if (!p) return 0; d->mlx_softmax_axis = (int (*)(mlx_array* res, const mlx_array a, int axis, bool precise, const mlx_stream s))p;
  p = dlsym(handle, "mlx_split");
  if (!p) return 0; d->mlx_split = (int (*)(mlx_vector_array* res, const mlx_array a, int num_splits, int axis, const mlx_stream s))p;
  p = dlsym(handle, "mlx_split_sections");
  if (!p) return 0; d->mlx_split_sections = (int (*)(mlx_vector_array* res, const mlx_array a, const int* indices, size_t indices_num, int axis, const mlx_stream s))p;
  p = dlsym(handle, "mlx_sqrt");
  if (!p) return 0; d->mlx_sqrt = (int (*)(mlx_array* res, const mlx_array a, const mlx_stream s))p;
  p = dlsym(handle, "mlx_square");
  if (!p) return 0; d->mlx_square = (int (*)(mlx_array* res, const mlx_array a, const mlx_stream s))p;
  p = dlsym(handle, "mlx_squeeze_axis");
  if (!p) return 0; d->mlx_squeeze_axis = (int (*)(mlx_array* res, const mlx_array a, int axis, const mlx_stream s))p;
  p = dlsym(handle, "mlx_stack");
  if (!p) return 0; d->mlx_stack = (int (*)(mlx_array* res, const mlx_vector_array arrays, const mlx_stream s))p;
  p = dlsym(handle, "mlx_stream_free");
  if (!p) return 0; d->mlx_stream_free = (int (*)(mlx_stream stream))p;
  p = dlsym(handle, "mlx_stream_new_device");
  if (!p) return 0; d->mlx_stream_new_device = (mlx_stream (*)(mlx_device dev))p;
  p = dlsym(handle, "mlx_subtract");
  if (!p) return 0; d->mlx_subtract = (int (*)(mlx_array* res, const mlx_array a, const mlx_array b, const mlx_stream s))p;
  p = dlsym(handle, "mlx_sum_axes");
  if (!p) return 0; d->mlx_sum_axes = (int (*)(mlx_array* res, const mlx_array a, const int* axes, size_t axes_num, bool keepdims, const mlx_stream s))p;
  p = dlsym(handle, "mlx_synchronize");
  if (!p) return 0; d->mlx_synchronize = (int (*)(mlx_stream stream))p;
  p = dlsym(handle, "mlx_take_along_axis");
  if (!p) return 0; d->mlx_take_along_axis = (int (*)(mlx_array* res, const mlx_array a, const mlx_array indices, int axis, const mlx_stream s))p;
  p = dlsym(handle, "mlx_tanh");
  if (!p) return 0; d->mlx_tanh = (int (*)(mlx_array* res, const mlx_array a, const mlx_stream s))p;
  p = dlsym(handle, "mlx_transpose");
  if (!p) return 0; d->mlx_transpose = (int (*)(mlx_array* res, const mlx_array a, const mlx_stream s))p;
  p = dlsym(handle, "mlx_transpose_axes");
  if (!p) return 0; d->mlx_transpose_axes = (int (*)(mlx_array* res, const mlx_array a, const int* axes, size_t axes_num, const mlx_stream s))p;
  p = dlsym(handle, "mlx_tril");
  if (!p) return 0; d->mlx_tril = (int (*)(mlx_array* res, const mlx_array x, int k, const mlx_stream s))p;
  p = dlsym(handle, "mlx_vector_array_free");
  if (!p) return 0; d->mlx_vector_array_free = (int (*)(mlx_vector_array vec))p;
  p = dlsym(handle, "mlx_vector_array_get");
  if (!p) return 0; d->mlx_vector_array_get = (int (*)(mlx_array* res, const mlx_vector_array vec, size_t idx))p;
  p = dlsym(handle, "mlx_vector_array_new");
  if (!p) return 0; d->mlx_vector_array_new = (mlx_vector_array (*)(void))p;
  p = dlsym(handle, "mlx_vector_array_new_data");
  if (!p) return 0; d->mlx_vector_array_new_data = (mlx_vector_array (*)(const mlx_array* data, size_t size))p;
  p = dlsym(handle, "mlx_vector_array_new_value");
  if (!p) return 0; d->mlx_vector_array_new_value = (mlx_vector_array (*)(const mlx_array val))p;
  p = dlsym(handle, "mlx_vector_array_size");
  if (!p) return 0; d->mlx_vector_array_size = (size_t (*)(mlx_vector_array vec))p;
  p = dlsym(handle, "mlx_vector_string_free");
  if (!p) return 0; d->mlx_vector_string_free = (int (*)(mlx_vector_string vec))p;
  p = dlsym(handle, "mlx_vector_string_new");
  if (!p) return 0; d->mlx_vector_string_new = (mlx_vector_string (*)(void))p;
  p = dlsym(handle, "mlx_vector_string_new_data");
  if (!p) return 0; d->mlx_vector_string_new_data = (mlx_vector_string (*)(const char** data, size_t size))p;
  p = dlsym(handle, "mlx_where");
  if (!p) return 0; d->mlx_where = (int (*)(mlx_array* res, const mlx_array condition, const mlx_array x, const mlx_array y, const mlx_stream s))p;
  p = dlsym(handle, "mlx_zeros");
  if (!p) return 0; d->mlx_zeros = (int (*)(mlx_array* res, const int* shape, size_t shape_num, mlx_dtype dtype, const mlx_stream s))p;
  return 1;
}
